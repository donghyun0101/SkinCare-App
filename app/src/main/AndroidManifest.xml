<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iconai.skincare"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkinCare">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.SkinTypeActivity" />
        <activity android:name=".activity.FindSkinTypeActivity" />
        <activity android:name=".activity.SkinConcernsActivity" />
        <activity android:name=".activity.GenderActivity" />
        <activity android:name=".activity.AgeActivity" />
        <activity android:name=".activity.DataCheckActivity" />
        <activity android:name=".activity.ImageCheckActivity" />
        <activity android:name=".activity.ResultActivity" />
        <activity android:name=".activity.CameraActivity" />

    </application>

</manifest>